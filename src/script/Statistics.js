import CARDS from './cards.js';

export default class Statistics {
  constructor(statistics) {
    this.statistics = statistics;
  }

  createStatistics(category) {
    console.log('Тут будет статистика!');
  }

}